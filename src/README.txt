This project takes a linked list of numbers, and will search it for a random amount of random numbers.

I made a Node struct, and used that to make my linked list. I used a for-loop to populate it with the random numbers.